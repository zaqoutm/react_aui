### Npm install

#### force install. they are using react:16

`npm i --force `

### Install packages

`npm i package name --force `

or

`npm i @atlaskit/button --legacy-peer-deps`

### Start

`npm start`

### Build

`npm run build`
